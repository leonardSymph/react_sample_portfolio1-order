import classes from "./MealsSummary.module.css";

const MealsSummary = () => {
  return (
    <section className={classes.summary}>
      <h2>Just a simple React App #2</h2>
      <p>you can add cart and you can order, only using firebase</p>
      <p>You can check the data output for your orders in console</p>
      <p>for portfolio purposes only</p>
    </section>
  );
};

export default MealsSummary;
